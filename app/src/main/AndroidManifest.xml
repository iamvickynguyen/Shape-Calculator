<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.day9_nhatnghe">

    <application
        android:allowBackup="true"
        android:icon="@drawable/shapes"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/trucvien">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity_hinhchunhat" />
        <activity android:name=".MainActivity_hinhvuong" />
        <activity android:name=".MainActivity_hinhtamgiac"></activity>
    </application>

</manifest>